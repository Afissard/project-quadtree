package bsp_dungeon

import "fmt"

/*
Fonction pour créer et remplire le contenu des couloirs
Algo :
Pacourt l'arbre BSP pour créer un couloir entre chaque node voisine
*/
func (level BSP_tree) createCorridor() {
	fmt.Println(level) // juste pour eviter erreur de compilation
}
